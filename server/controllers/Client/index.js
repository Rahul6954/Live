const { ClientGeneral, ClientCreate, ClientData,recordEdit, recordDelete } = require('./ClientControllers');

module.exports = {
  ClientGeneral,
  ClientCreate,
  ClientData,
  recordEdit,
  recordDelete,
};
